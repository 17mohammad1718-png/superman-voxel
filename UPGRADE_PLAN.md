# 🗺️ پلن ارتقای Superman Voxel

> سند پیشنهادی برای ارتقای بازی — بر اساس وضعیت *واقعی* کد در `index.html` (۱۱۴۰ خط، single-file).
> هدف: از «شبیه‌ساز تخریب با سه قدرت» به یک بازیِ کامل‌تر، روان‌تر و جذاب‌تر برسیم.

---

## ۰) وضعیت فعلی (تا چیزی که انجام شده دوباره پیشنهاد نشود)

چیزهایی که از قبل در کد هست و در این پلن **تکرار نمی‌شوند**:

- ✅ `InstancedMesh` به ازای (چانک، نوع بلوک) → ~۵۰ draw call به‌جای ~۱۱٬۰۰۰
- ✅ ری‌کست لیزر با DDA (آماناتیدس–وو) به‌جای raycast روی کل صحنه
- ✅ بازسازی تنبل چانک‌ها (`dirtyChunks` + بودجهٔ ۲ چانک در هر فریم)
- ✅ آب، درخت‌های بدون بریدگی مرز چانک، گرانش/پرش در حالت پیاده
- ✅ seed قطعی در URL، شمارندهٔ FPS، مینی‌مپ با heightmap کش‌شده
- ✅ رفع باگ pause/کلیدهای گیرکرده، رفع باگ متریال X-Ray
- ✅ پنل خطای دقیق boot + وندور لوکال Three.js (بدون CDN)

**نتیجه:** گلوگاه اصلی قبلی (مش‌سازی per-block) رفع شده. گلوگاه باقی‌مانده، **تعداد مثلث‌ها** و **محتوای/عمق بازی** است.

---

## ۱) فاز صفر — بردهای سریع (کم‌هزینه، اثر فوری) 🟢

این‌ها را می‌شود در یک نشست کوتاه انجام داد و بلافاصله حس «بازی واقعی» می‌دهند.

### ۱.۱ فارسی‌سازی UI
- **چرا؟** صفحه `lang="fa" dir="rtl"` است ولی همهٔ متن‌ها انگلیسی‌اند (عنوان، دکمه‌ها، کنترل‌ها، HUD، پنل خطا).
- **چطور:** رشته‌های ثابت را فارسی کن + پنل خطای boot هم فارسی شود (الان کاربر فارسی‌زبان پیام انگلیسی می‌گیرد).
- **اثر/تلاش:** بالا / ~۱–۲ ساعت

### ۱.۲ صدا با WebAudio (بدون فایل خارجی)
- **چرا؟** بازی کاملاً بی‌صدا است — هیچ `AudioContext`ای نیست. صدا حس قدرت را چند برابر می‌کند.
- **چطور:** یک ماژول کوچک صدا که با اسیلاتور/نویز این‌ها را بسازد:
  - لیزر: sweep فرکانس بالا + نویز (۰.۱ ثانیه)
  - شکستن بلوک: نویز کوتاه با pitch بر اساس نوع بلوک (سنگ بم‌تر، شیشه/برگ زیرتر)
  - پرواز/هول: نویز باندی که با سرعت `S.flySpeed` بالا/پایین می‌رود
  - پرش و فرود
- **نکته:** باید بعد از اولین کلیک کاربر (autoplay policy) روشن شود.
- **اثر/تلاش:** خیلی بالا / ~۳–۴ ساعت

### ۱.۳ ذخیرهٔ پیشرفت در localStorage
- **چرا؟** تخریب‌ها و موقعیت بعد از refresh از بین می‌روند. با seed دنیا قابل بازتولید است، پس فقط باید *دلتاها* ذخیره شوند.
- **چطور:** یک لیست `[wx,wy,wz]` از بلوک‌های تخریب‌شده + موقعیت/زاویهٔ بازیکن + `destroyed`. موقع load: دنیا را از seed بساز، بعد دلتاها را اعمال کن.
- **اثر/تلاش:** متوسط–بالا / ~۲–۳ ساعت

### ۱.۴ اسلایدر کیفیت / تنظیمات
- **چرا؟** `pixelRatio` تا ۲ قفل است، سایه ۲۰۴۸×۲۰۴۸ همیشه روشن. دستگاه‌های ضعیف (یا موبایل) اذیت می‌شوند.
- **چطور:** منوی Pause با چند گزینه: کیفیت سایه (خاموش/۱۰۲۴/۲۰۴۸)، pixelRatio (۱/۲)، فاصلهٔ دید fog، FOV.
- **اثر/تلاش:** متوسط / ~۲ ساعت

---

## ۲) فاز یک — عملکرد و مقیاس 🔴

### ۲.۱ Greedy Meshing (بزرگ‌ترین جهش باقی‌مانده)
- **چرا؟** الان هر بلوکِ نمایان یک مکعب کامل `BoxGeometry` است؛ `hidden()` فقط بلوک‌های «کاملاً محصور» را حذف می‌کند، ولی دو بلوک هم‌سطح روی زمین **وجهِ مشترکِ بینِ خودشان را هم رندر می‌کنند**. یعنی مثلث‌های بیهودهٔ زیادی هست (~۱۳۱ هزار مثلث → حدود ~۲۶ هزار با greedy).
- **چطور:** به‌جای instancing مکعب، وجه‌های نمایان هر چانک را در یک `BufferGeometry` واحد ادغام کن (به ازای هر وجهِ هم‌سطحِ هم‌رنگ، یک quad بزرگ بساز). خروجی: **۱ draw call به ازای هر چانک** به‌جای ~۵۰.
- **وابستگی:** پیش‌نیاز فاز «دنیای بزرگ‌تر».
- **اثر/تلاش:** خیلی بالا / متوسط–زیاد (~۱–۲ روز)

### ۲.۲ دنیای بزرگ‌تر + استریم چانک
- **چرا؟** الان دنیا ۴×۴ چانک (۶۴×۶۴) است؛ مرزها خیلی زود دیده می‌شوند.
- **چطور:** بعد از greedy meshing، دنیا را به ۱۶×۱۶ چانک (۲۵۶×۲۵۶) یا حتی بی‌نهایت با تولید تدریجی اطراف بازیکن ببر. `WCX/WCZ` از قبل ثابت‌اند؛ فقط تولید و ایندکس باید به مختصات چانکِ دلخواه تبدیل شوند.
- **اثر/تلاش:** بالا / زیاد (به greedy وابسته است)

### ۲.۳ تولید/مش‌سازی در Web Worker
- **چرا؟** `genTerrain` و `buildAll` روی نخ اصلی اجرا می‌شوند و load اولیه (و بازسازی چانک‌های بزرگ) مکث می‌دهد.
- **چطور:** نویز + ساخت آرایهٔ بلوک‌ها در Worker (نویز deterministic است، پس seed را پاس بده) و فقط `ArrayBuffer` نتیجه را برگردان. مش‌سازی هندسه هم قابل انتقال است.
- **اثر/تلاش:** بالا / متوسط

### ۲.۴ بافت + سایه‌زنی وجه‌ها (look واقعی)
- **چرا؟** همهٔ بلوک‌ها رنگ تخت‌اند (`MeshLambertMaterial` با یک رنگ). چمن/سنگ/چوب واقعی‌تر می‌شوند.
- **چطور:** یک **texture atlas** کوچک (کاشی ۱۶×۱۶) به‌صورت Canvas-generated (بدون فایل خارجی) + UV هر وجه. در greedy meshing هر وجه می‌تواند tile خودش را بگیرد. به‌علاوه **AO لبه‌ها** و نور سادهٔ سمت‌دار برای عمق.
- **اثر/تلاش:** بالا / متوسط–زیاد

---

## ۳) فاز دو — گیم‌پلی و محتوا 🟠

### ۳.۱ قدرت‌های جدید (سیستم `PDATA` آمادهٔ توسعه است)
| قدرت | کلید | کار | پیچیدگی |
|---|---|---|---|
| ❄️ نفس یخی | `1` | بلوک‌های جلوی بازیکن (شعاعی) → SNOW/ICE | کم |
| 👊 مشت ضربتی | `2` | تخریب شعاعی (کرهٔ ۲–۳ بلوکی) | کم |
| 🔥 دید حرارتی | `3` | بلوک‌های عمیق‌تر داغ/درخشان دیده شوند | متوسط |
| ⚡ سرعت فوق‌العاده | `4` | ضریب سرعت ×۳ با افکت motion | کم |

- **نکته:** برای قدرت‌هایی که «شعاع» می‌زنند، `destroyBlock` را در یک حلقهٔ شعاعی صدا بزن (dirty flag خودش بازسازی را amortized می‌کند).

### ۳.۲ گذاشتن بلوک (کلیک راست)
- **چرا؟** دنیا فقط تخریب‌شدنی است؛ بازیکن نمی‌تواند بسازد.
- **چطور:** با `contextmenu` روی بلوک مجاورِ face نرمالِ hit (که `voxelRaycast` از قبل `nx,ny,nz` برمی‌گرداند) بلوک بگذار. + یک hotbar ساده برای انتخاب نوع بلوک.
- **اثر/تلاش:** بالا / کم–متوسط

### ۳.۳ چرخهٔ شب و روز
- **چرا؟** آسمان و نور ثابت‌اند.
- **چطور:** زاویهٔ `sunLight` در طول یک چرخه (مثلاً ۴ دقیقه) بچرخد و رنگ `scene.background`/`scene.fog`/نور محیط بین روز و شب lerp شود. ستاره/ماه به‌صورت آسمانِ ساده.
- **اثر/تلاش:** بالا / متوسط

### ۳.۴ هدف، امتیاز و مأموریت
- **چرا؟** الان بازی «sandbox بدون هدف» است؛ بعد از ۵ دقیقه حوصله‌سربر می‌شود.
- **چطور:** یک هدف اولیهٔ ساده و مؤثر:
  - **جمع‌آوری سنگ معدن:** شمردن آهن/الماس تخریب‌شده + نمایش در HUD + امتیاز
  - مأموریت‌های کوتاه («۵ الماس پیدا کن») با toast
  - **highscore** در localStorage
- **اثر/تلاش:** بالا / متوسط

---

## ۴) فاز سه — پولیش و پلتفرم 🟡

### ۴.۱ کنترل لمسی/موبایل
- **چرا؟** روی موبایل اصلاً قابل بازی نیست.
- **چطور:** جوی‌استیک مجازی چپ (حرکت) + درگ راست (دوربین) + دکمه‌های قدرت. نیاز به detect `touch` و فعال‌کردن fallback درگ (که از قبل وجود دارد: `dragLook`).

### ۴.۲ ذرات و افکت
- شکستن بلوک: چند cube کوچک که با گرانش می‌پاشند (یک `InstancedMesh` ذره).
- برخورد لیزر: اسپارک، دود، موج نور.
- برف/باران ساده.

### ۴.۳ آسمان و ابرها
- به‌جای رنگ تخت، گرادیان آسمان + ابرهای صافِ متحرک (چند plane).

### ۴.۴ مینی‌مپ پیشرفته
- نمایش جهت شمال، موقعیت سنگ معدن کشف‌شده (با X-Ray)، زوم.

---

## ۵) فاز چهار — بلندپروازانه 🔵 (بعد از بقیه)

- **موجودات/دشمن:** یک موجود سادهٔ voxel که حرکت می‌کند و باید از آن فرار کنی یا با لیزر بزنی — بازی را از «شبیه‌ساز» به «بازی» تبدیل می‌کند.
- **چندنفره/اشتراک:** اشتراک seed از قبل هست؛ sync موقعیت از طریق WebSocket خیلی بزرگ است.
- **آب واقعی:** سطح متحرک، شناوری، ورود/خروج به آب.
- **ذخیره/لود دنیای کامل** در یک فرمت فشرده (RLE) به‌جای لیست دلتاها.

---

## ۶) اولویت‌بندی نهایی

| اولویت | کار | اثر | تلاش | وابستگی |
|---|---|---|---|---|
| 🟢 P0 | صدا (WebAudio) | حس بازی ×۲ | کم | — |
| 🟢 P0 | فارسی‌سازی UI | UX | خیلی کم | — |
| 🟢 P0 | ذخیرهٔ پیشرفت | بازگشت کاربر | کم | — |
| 🔴 P1 | Greedy Meshing | FPS + مقیاس | متوسط–زیاد | — |
| 🟠 P1 | قدرت‌های جدید + گذاشتن بلوک | عمق بازی | کم–متوسط | — |
| 🟠 P1 | هدف/امتیاز | انگیزه | متوسط | ذخیره |
| 🟡 P2 | چرخهٔ شب و روز | فضا | متوسط | — |
| 🔴 P2 | دنیای بزرگ‌تر + Worker | مقیاس | زیاد | greedy |
| 🟡 P2 | ذرات/آسمان | پولیش | کم–متوسط | — |
| 🟡 P3 | موبایل | دسترسی | متوسط | — |
| 🔵 P4 | دشمن/چندنفره | تعریف بازی | زیاد | greedy |

---

## ۷) ترتیب پیشنهادی اجرا

1. **اسپرینت ۱ (۱ روز):** صدا + فارسی‌سازی + ذخیرهٔ پیشرفت → بازی «کامل» به نظر می‌رسد.
2. **اسپرینت ۲ (۲–۳ روز):** Greedy Meshing + بافت + آسمان → روان‌تر و زیباتر.
3. **اسپرینت ۳ (۲–۳ روز):** قدرت‌های جدید + گذاشتن بلوک + امتیاز/مأموریت → عمق گیم‌پلی.
4. **اسپرینت ۴ (۲ روز):** دنیای بزرگ‌تر + Worker + موبایل.
5. **اسپرینت ۵ (آینده):** دشمن، آب واقعی، ذخیرهٔ فشرده.

> **پیشنهاد:** از اسپرینت ۱ شروع کن؛ کم‌ترین ریسک و بیشترین «حس ارتقا» را دارد و هیچ‌کدام به بازنویسی موتور (greedy) وابسته نیستند.
