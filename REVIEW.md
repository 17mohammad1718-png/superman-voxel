# 🔍 بررسی فنی پروژه Superman Voxel

بررسی روی `index.html` (۷۷۶ خط، single-file) انجام شد. موارد زیر به ترتیب اولویت دسته‌بندی شده‌اند.

---

## ۱) باگ‌های واقعی (اول اینها را درست کن)

### 🐞 X-Ray مادهٔ اصلی را ذخیره نمی‌کند
```js
if (m) { m.material = getXrayMat(b.type); m.userData._origMat = m.material; }
```
اول متریال جایگزین می‌شود و بعد «متریال اصلی» ذخیره می‌شود — یعنی همان متریال شفاف ذخیره می‌شود. `_origMat` عملاً بی‌فایده است. `disableXray` هم به `ch.blockMats[type]` تکیه می‌کند که اگر آن نوع بلوک بعد از rebuild ساخته نشده باشد، `undefined` است و بلوک برای همیشه شفاف می‌ماند.
**درست:** `m.userData._origMat = m.material;` را **قبل** از جایگزینی بگذار و در disable از همان استفاده کن.

### 🐞 `buildMeshIndex` بدنهٔ بازیکن را هم ایندکس می‌کند
`scene.traverse` روی **همهٔ** مش‌ها می‌رود — شامل بدن، شنل، بوت‌ها، `hitFlash` و `glowMesh`. چون کلید ایندکس مختصات گرد‌شده است، این مش‌ها ورودی بلوک هم‌مختصات را overwrite می‌کنند و X-Ray روی بلوک زیر پای بازیکن اشتباه عمل می‌کند (و می‌تواند شنل سوپرمن را شفاف کند).
**درست:** فقط روی `chunk.group` ها traverse کن یا مش‌ها را با `userData.isBlock = true` علامت بزن.

### 🐞 کلیدها بعد از از دست رفتن pointer-lock گیر می‌کنند
`S.paused` فقط `false` می‌شود و هیچ‌جا دوباره `true` نمی‌شود. با ESC، بازی ادامه دارد و اگر W نگه داشته شده بود، `keyup` دریافت نمی‌شود و بازیکن تا ابد حرکت می‌کند.
**درست:**
```js
document.addEventListener('pointerlockchange', () => {
  locked = !!document.pointerLockElement;
  S.paused = !locked;
  if (!locked) S.keys = {};
});
window.addEventListener('blur', () => { S.keys = {}; S.paused = true; });
```

### 🐞 آب هرگز تولید نمی‌شود
`BT.WATER` تعریف شده، رنگ و متریال شفاف دارد، در `collides` استثنا شده و README هم آن را وعده داده — ولی در `genTerrain` هیچ‌جا set نمی‌شود. یک sea level ساده لازم است:
```js
const SEA = 9;
for (let y = h+1; y <= SEA; y++) ch.set(x, y, z, BT.WATER);
```

### 🐞 درخت‌ها روی مرز چانک بریده می‌شوند
برگ‌ها با `ch.set(x+dx, ..., z+dz)` نوشته می‌شوند و `set` مختصات بیرون از چانک را **بی‌صدا دور می‌ریزد**. درخت‌های نزدیک لبه نصفه می‌شوند.
**درست:** تولید را روی مختصات جهانی انجام بده (`world.setRaw(wx,wy,wz)`) یا مرحلهٔ درخت‌کاری را بعد از ساخت همهٔ چانک‌ها اجرا کن.

### 🐞 حالت «پیاده» (Q) با حالت پرواز فرقی ندارد
هیچ گرانش/زمین‌گیری‌ای وجود ندارد؛ `Q` فقط لیبل HUD را عوض می‌کند. یا گرانش اضافه کن یا اسمش را عوض کن.

---

## ۲) عملکرد — بزرگ‌ترین فرصت پروژه

اندازه‌گیری واقعی روی همین الگوریتم تولید دنیا (۶۴×۳۲×۶۴):

| معیار | مقدار فعلی |
|---|---|
| بلوک توپر | ۵۲٬۸۴۷ |
| بلوک نمایان (بعد از cull) | **۱۰٬۹۴۶ مش مجزا** |
| مثلث | ~۱۳۱٬۰۰۰ |
| مثلث با greedy meshing | **~۲۶٬۰۰۰ (۵ برابر کمتر)** |

### 🔴 ~۱۱٬۰۰۰ draw call در هر فریم
```js
const inst = this.meshes[t].clone();
this.group.add(inst);
```
هر بلوک یک `THREE.Mesh` مستقل است. این گران‌ترین اشتباه ممکن در three.js است — CPU در حلقهٔ رندر خفه می‌شود، نه GPU.

**راه‌حل الف (کم‌هزینه، ~۱ ساعت کار):** `THREE.InstancedMesh` — یک draw call به ازای هر «نوع بلوک در هر چانک» → از ۱۱٬۰۰۰ به **~۵۰** draw call.

**راه‌حل ب (بهترین، استاندارد صنعت):** greedy meshing — به‌جای مکعب، فقط وجه‌های نمایان را در یک `BufferGeometry` ادغام کن → **۱ draw call به ازای هر چانک** و ۵ برابر مثلث کمتر. با این کار می‌توانی دنیا را از ۴×۴ به ۱۶×۱۶ چانک بزرگ کنی و همچنان ۶۰fps داشته باشی.

### 🔴 Raycast لیزر روی کل صحنه
```js
_ray.intersectObjects(scene.children, true);
```
یعنی تست تقاطع با ۱۱٬۰۰۰ مش در هر شلیک → فریز محسوس.
**درست:** DDA/Amanatides–Woo روی گرید ووکسل. چون خودت آرایهٔ بلوک‌ها را داری، هزینه `O(طول پرتو)` ≈ ۵۰ گام می‌شود، نه `O(تعداد مش‌ها)`. علاوه بر این دقیق‌تر است (الان با `hit.point - dir*0.5` بلوک را حدس می‌زند که روی لبه‌ها اشتباه می‌کند).

### 🔴 هر تخریب بلوک = بازسازی کل چانک + بازسازی کل ایندکس
```js
rebuild(ch) { ch.dispose(scene); ch.build(scene); buildMeshIndex(); }
```
`buildMeshIndex` کل صحنه را traverse می‌کند. با لیزرِ اتوماتیک این یعنی چند ده میلی‌ثانیه استال به ازای هر بلوک.
**درست:** چانک را `dirty` علامت بزن و حداکثر یک‌بار در فریم (یا در چند فریم) بازسازی کن — فیلد `dirty` از قبل هست ولی هیچ‌وقت استفاده نمی‌شود.

### 🟡 نشتی حافظهٔ GPU
`group.clear()` روی geometry/material `dispose()` صدا نمی‌زند. چون geometry مشترک است فاجعه نیست، ولی با مهاجرت به BufferGeometry به‌ازای چانک، حتماً باید `dispose()` صریح اضافه شود.

### 🟡 Minimap: ~۱۱۵٬۰۰۰ `getBlock` هر ۱۵ فریم
اسکن کامل ستون از `MAX_H` تا ۰ برای ۶۰×۶۰ خانه. یک **heightmap کش‌شده** (`Uint8Array(WX*WZ)`) که فقط هنگام تخریب بلوک آپدیت شود، این را تقریباً صفر می‌کند.

### 🟡 سایه‌ها عملاً هدر می‌روند
`castShadow = false` روی همهٔ بلوک‌ها ست شده، یعنی هیچ بلوکی سایه نمی‌اندازد — ولی shadow map با رزولوشن ۲۰۴۸×۲۰۴۸ و frustum ۱۲۰ واحدی (دو برابر اندازهٔ دنیا) هر فریم رندر می‌شود. یا `castShadow` را روشن کن، یا frustum را به اندازهٔ دنیا (۶۴) تنگ کن تا کیفیت سایه دو برابر شود، یا اگر سایه لازم نیست کلاً خاموشش کن.

### 🟡 خواندن DOM در حلقهٔ رندر
`document.getElementById(...)` در `updateHUD` هر ۵ فریم و در `setPower`. رفرنس‌ها را یک‌بار در بالای فایل کش کن.

---

## ۳) کد مرده و ناسازگاری‌ها

| مورد | وضعیت |
|---|---|
| `BLOCK_NAMES` | تعریف شده، هیچ‌جا استفاده نشده |
| `S.mouseX`, `S.mouseY`, `S.camAngle` | همیشه صفر، بی‌استفاده |
| `chunk.dirty` | ست می‌شود ولی هرگز خوانده نمی‌شود |
| `chunk.meshes` | بعد از build دیگر استفاده نمی‌شود |
| `m.userData._origMat` | نوشته می‌شود، خوانده نمی‌شود |
| README: «Perlin noise» | در متن فارسی «ترافیک» نوشته شده (احتمالاً غلط تایپی «توپوگرافی») |
| README: مسیر `H:/superman-voxel/` | مسیر ویندوزی لوکال، باید حذف شود |
| README: نرخ سنگ معدن | ۱.۵٪ و ۰.۳٪ ذکر شده و با کد می‌خواند ✅ |

---

## ۴) ارتقاهای پیشنهادی (ویژگی)

**کم‌هزینه، اثر زیاد:**
1. **Seed در URL** (`?seed=1234`) — الان `Math.random()` است و هیچ دنیایی قابل بازتولید/اشتراک نیست.
2. **شمارندهٔ FPS** در HUD — بدون آن هیچ بهینه‌سازی‌ای قابل سنجش نیست.
3. **منوی Pause واقعی** با ESC (الان ESC فقط pointer-lock را می‌بندد و بازی ادامه دارد).
4. **صدا** — ووش پرواز، صدای لیزر، شکستن بلوک. با WebAudio بدون فایل خارجی هم شدنی است.
5. **تنگ کردن fog به فاصلهٔ دید واقعی** و افزودن اسلایدر کیفیت (سایه/pixelRatio) برای دستگاه‌های ضعیف.

**متوسط:**
6. **قدرت‌های بیشتر:** نفس یخی (تبدیل بلوک به SNOW/ICE)، سوپر شنوایی، مشت ضربتی (تخریب شعاعی) — سیستم `PDATA` از قبل قابل توسعه است.
7. **گذاشتن بلوک** با کلیک راست — دنیا الان فقط تخریب‌شدنی است.
8. **چرخهٔ شب و روز** — با حرکت `sunLight` و lerp رنگ `scene.background`/fog.
9. **ذخیرهٔ پیشرفت** در `localStorage` (بلوک‌های تخریب‌شده + موقعیت).
10. **کنترل لمسی/موبایل** — صفحه `dir="rtl"` است ولی روی موبایل اصلاً قابل بازی نیست.

**زیرساختی:**
11. **UI فارسی**: صفحه `lang="fa" dir="rtl"` است ولی تمام متن‌ها انگلیسی‌اند. یا هر دو را فارسی کن یا `lang="en" dir="ltr"`.
12. **CDN بدون fallback**: اگر unpkg در دسترس نباشد صفحهٔ سفید می‌گیری، بدون هیچ پیام خطایی. یک `try/catch` + پیام خطا اضافه کن (و در حالت ایده‌آل three.js را لوکال بیاور).
13. **شکستن به ماژول**: `noise.js`, `chunk.js`, `world.js`, `player.js`, `powers.js`, `ui.js`. ES Modules از قبل فعال است، پس بدون build step هم شدنی است.
14. **بدون `.gitignore`، بدون لایسنس، بدون CI** — حداقل یک `.gitignore` و لایسنس اضافه شود.

---

## ۵) خلاصهٔ اولویت‌بندی

| اولویت | کار | اثر | تلاش |
|---|---|---|---|
| 🔴 P0 | InstancedMesh یا greedy meshing | ۱۱٬۰۰۰ → ۵۰ draw call | متوسط |
| 🔴 P0 | Voxel DDA به‌جای raycast صحنه | حذف فریز لیزر | کم |
| 🔴 P0 | rebuild تنبل با فلگ `dirty` | حذف استال تخریب | کم |
| 🔴 P0 | رفع باگ ذخیرهٔ متریال X-Ray | باگ منطقی | خیلی کم |
| 🟠 P1 | pause/blur درست + پاک کردن کلیدها | باگ کنترل | خیلی کم |
| 🟠 P1 | فیلتر ایندکس مش به بلوک‌ها | باگ X-Ray | خیلی کم |
| 🟠 P1 | کش heightmap برای minimap | حذف ۱۱۵k lookup | کم |
| 🟠 P1 | آب + درخت‌های مرزی | درستی دنیا | کم |
| 🟡 P2 | seed در URL، FPS، منوی pause | UX | کم |
| 🟡 P2 | تنظیم/حذف سایه | ~۱۰-۲۰٪ فریم | خیلی کم |
| 🟢 P3 | ماژولار کردن، صدا، قدرت‌های جدید | نگه‌داری/محتوا | زیاد |

**جمع‌بندی:** معماری کلی (chunk + Uint8Array + face culling + spatial index) درست انتخاب شده؛ گلوگاه اصلی این است که مرحلهٔ «مش‌سازی» هنوز per-block است. با P0ها می‌توانی هم‌زمان هم روان‌تر شوی و هم دنیا را چند برابر بزرگ‌تر کنی.
